# Versions figees sur l'environnement de developpement valide (pip list).
# =============================================================================
#  Serveur MCP (src/mcp_server.py) — le minimum pour le conteneur Docker / HF
# =============================================================================
mcp==1.28.0
duckdb==1.5.0

# =============================================================================
#  Interfaces
# =============================================================================
# Streamlit (app.py)
streamlit==1.58.0
pandas==3.0.1
matplotlib==3.10.8

# FastAPI (api.py + static/index.html)
fastapi==0.141.1
uvicorn[standard]==0.49.0

# =============================================================================
#  Backends de modele (src/backends.py) — un seul est necessaire a la fois
# =============================================================================
ollama==0.6.2                 # DAMIA_BACKEND=ollama   (defaut, local)
openai==2.29.0                # DAMIA_BACKEND=openai   (Groq, Mistral, OpenRouter...)
httpx==0.28.1                 # transport HTTP partage (client MCP + backends)

# Optionnels : decommenter selon le backend utilise
# anthropic==0.112.0          # DAMIA_BACKEND=api
# google-generativeai==0.8.6  # DAMIA_BACKEND=gemini
# torch                       # DAMIA_BACKEND=transformers (GPU recommande)
# transformers
# bitsandbytes

# NB : pour refiger apres une mise a jour -> pip freeze | findstr /I "mcp openai duckdb"
